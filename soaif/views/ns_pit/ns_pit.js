//meteor add mrt:underscore-inflection

if (Meteor.isClient) {
  Tracker.autorun(function() {  
    
  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });

  //Router.route('/help');
}